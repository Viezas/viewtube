import { VideoThumbnailPipe } from './video-thumbnail.pipe';

describe('VideoThumbnailPipe', () => {
  it('create an instance', () => {
    const pipe = new VideoThumbnailPipe();
    expect(pipe).toBeTruthy();
  });
});
